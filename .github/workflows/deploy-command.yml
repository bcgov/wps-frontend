name: deploy-command

on:
  repository_dispatch:
    types: [deploy-command]

jobs:
  # add_reaction:
  #   name: Add reaction
  #   runs-on: ubuntu-latest
  #   # Use this as an initial start point
  #   steps:
  #     - name: Add reaction
  #       uses: peter-evans/create-or-update-comment@v1
  #       with:
  #         token: ${{ secrets.REPO_ACCESS_TOKEN }}
  #         repository: ${{ github.event.client_payload.github.payload.repository.full_name }}
  #         comment-id: ${{ github.event.client_payload.github.payload.comment.id }}
  #         reaction-type: hooray

  deploy_prod:
    name: Deploy to prod
    runs-on: ubuntu-latest
    steps:
      - name: Output command and arguments
        run: |
          echo ${{ github.event.client_payload.slash_command.command }}
          echo ${{ github.event.client_payload.slash_command.args }}
          echo ${{ github.event.client_payload.slash_command.arg1 }}
          echo ${{ github.event.client_payload.slash_command.arg1 == prod }}
